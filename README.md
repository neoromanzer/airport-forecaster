# Airport Passenger Forecaster

A self-contained single-file web app for forecasting airport passenger volumes.

Open `airport-forecaster.html` directly in a browser — no build step or server needed.
